{
  "schemaVersion": 1,
  "title": "PeerSpot Scraper Input",
  "type": "object",
  "properties": {
    "productName": {
      "title": "Product Name",
      "type": "string",
      "description": "Name of the product to scrape reviews for (e.g., 'Microsoft Power BI', 'Fortinet FortiGate', 'VMware vSphere')",
      "editor": "textfield",
      "prefill": "Microsoft Power BI"
    },
    "maxReviews": {
      "title": "Max Reviews",
      "type": "integer",
      "description": "Maximum number of reviews to scrape per product",
      "default": 50,
      "minimum": 1,
      "maximum": 500,
      "prefill": 50
    },
    "includeComparisons": {
      "title": "Include Product Comparisons",
      "type": "boolean",
      "description": "Also scrape product comparison pages",
      "default": true,
      "prefill": true
    }
  },
  "required": ["productName"]
}
